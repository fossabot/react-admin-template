/**
 * 目前仅作为标题和面包屑导航配置对象的容器。
 * 不要在这个文件添加代码。
 * @inject('maps')(YourComponent)
 * this.props.maps.titleMap {[path: string]: [title: string]}
 * this.props.maps.breadcrumbNameMap {[path: string]: [breadcrumb: string]}
 * 目前唯一数据来源【routes/index.js】
 * 如有修改，请务必补充注释
 */

class Maps {}
export default new Maps();
